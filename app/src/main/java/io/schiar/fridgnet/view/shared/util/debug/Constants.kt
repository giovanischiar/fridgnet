package io.schiar.fridgnet.view.shared.util.debug

/**
 * Used to generate the unit tests of everything that is being drawn in the map component inside the
 * Regions and Images screen. For unit test generation purposes.
 */
const val generatePolygonsAppCreatedUnitTest = false

/**
 * Show data about all regions being drawn and regions that crossed the antimeridian (180th
 * longitude). For debugging purposes.
 */
const val showMapPolygonInfo = false

/**
 * Also draw the bounding box around the polygon. For debugging purposes.
 */
const val showBoundingBoxPolygon = false